version: 2.1

jobs:
  node-lts:
    docker:
      - image: cimg/node:lts
    steps:
      - run:
          name: "NodeのVersionを表示"
          command: |
            echo `node -v` 
  node-12.20:
    docker:
      - image: cimg/node:12.20
    steps:
      - run:
          name: "NodeのVersionを表示"
          command: |
            echo `node -v` 

workflows:
  build-test-deploy:
    jobs:
      - node-lts
      - node-12.20
