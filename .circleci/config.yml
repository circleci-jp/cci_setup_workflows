version: 2.1

setup: true

orbs:
  path-filtering: circleci/path-filtering@dev:alpha

workflows:
  setup-workflow:
    jobs:
      - path-filtering/filter:
          mapping: |
            lts/.* build-server true
            v15/.* build-client true
          # Optional, defaults to main:
          base-revision: origin/develop
