version: 2.1

setup: true

orbs:
  # path-filtering: circleci/path-filtering@dev:alpha
  continuation: circleci/continuation@0.1.2

jobs:
  pwd:
    docker:
        - image: cimg/node:lts
    steps:
      - run:
          command: |
            echo `pwd`

workflows:
  use-my-orb:
    jobs:
      - continuation/continue:
          configuration_path: ./A/.circleci/config.yml    
  # setup-workflow:
  #   jobs:
  #     - path-filtering/filter:
  #         mapping: |
  #           A/.* dir_A true
  #           B/.* dir_B true
  #         # Optional, defaults to main:
  #         # base-revision: origin/develop
